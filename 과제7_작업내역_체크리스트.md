# 과제 7 작업내역 체크리스트 — 잠긴 플랜두씨 다이어리 (SSOT)

이 파일 하나만 읽으면 과제 7의 현재 상태를 알 수 있어야 한다.
과제 6 기록은 `작업내역_체크리스트.md` 에 그대로 남아 있다. **지우지 않는다.**

## 1. 현재 상태

- **과제**: 과제 6 다이어리에 가입·로그인을 붙여 내 자료를 나만 보게 만들기
- **단계**: **A0~A9 완료, A10 날짜·규칙 순서 확인 완료.** 전체 API DTO 보강과 공개 재배포 완료, 2026-09-03 Claude 전면 재검증(자동 검사 4종 + 코드 감사 + 공개 배포 응답 + 문서 정합성) 통과 → 로그인이 필요한 양방향 Postman/DB 촬영과 C132는 사용자 직접 진행 대기(§6)
- **작업 위치**: `C:\Users\stuls\Desktop\Agent\plan-do-see-diary` (과제 6과 같은 폴더·같은 저장소·`main` 브랜치)
- **데이터베이스**: **과제 6 것을 그대로 공유.** 계정·세션 표와 소유자 컬럼 적용, 기존 계획 2·할 일 5·돌아보기 1을 소유자 계정에 귀속하고 `user_id NOT NULL` 확인
- **과제 6 상태**: 최종 승인 통과. 문서·보고서·증거는 모두 보존
- **이어받은 과제 6 고정 commit**: `356a46034e94084caec3e17eaaf2ab8d98cef7ce` (T07-C78 — `main` 이력의 조상)

### 로그인 방법 (확정)

| 방법 | 입력 | 상태 |
|---|---|---|
| 일반 | **아이디 · 비밀번호** | 구현 완료 (가입은 아이디·닉네임·이메일·비밀번호 4칸) |

### 과제 7 산출물 위치 (과제 6과 구분)

| 종류 | 과제 6 | 과제 7 |
|---|---|---|
| 진행 기록 | `작업내역_체크리스트.md` | **`과제7_작업내역_체크리스트.md`** (이 파일) |
| 문서 | `docs/*.md` | **`docs/과제7/*.md`** |
| 증거 | `docs/evidence/` | **`docs/과제7/evidence/`** |

### ⛔ 절대 지킬 것

| 금지 | 이유 |
|---|---|
| 과제 6 문서·보고서·증거 삭제 | 승인된 제출물이다. 과제 7은 별도 경로에 쌓는다 |
| `user_id` 를 처음부터 `NOT NULL` 로 추가 | 인증판 배포 전까지 과제 6 앱의 등록 기능이 깨진다 (기획 3-2 의 4단계 순서) |
| 비밀번호·토큰 원문을 코드·문서·로그에 남기기 | T07-C46 / C131 |
| 사용자 ID 를 URL·헤더·요청 본문에서 읽기 | T07-C123. 사용자는 **세션에서만** 확정한다 |
| 중복확인을 화면에서만 하고 DB 유니크 인덱스를 빼기 | 동시 가입 시 중복이 그대로 들어간다 |
| DB 행을 그대로 응답에 싣기 | 새 컬럼이 자동으로 실려 나간다. 반드시 `lib/dto/` 를 거친다 |

## 2. 진행 체크리스트

| 단계 | 내용 | 상태 | 담당 |
|---|---|---|---|
| **A0** | 과제 7 설계 확정, 문서 세트 구축 (`docs/과제7/`) | ☑ 완료 | Claude |
| **A1** | `schema_auth.sql` 작성 — `app_user`·`user_session` 표, 소유자 컬럼 4단계 마이그레이션 | ☑ 파일 작성·DB 확장 단계 적용 완료 | Claude + Codex |
| **A2** | 인증 코어 — `lib/domain/rules.ts`, `lib/repository/{user,session}.ts`, `lib/service/auth.ts`, `lib/session.ts`, `lib/dto/` | ☑ 완료 | Claude |
| **A3** | 화면·API — 로그인·가입 화면, `/api/auth/{check,signup,login,logout,password}`, `middleware.ts`, 계정 화면 | ☑ 완료 | Claude |
| **A4** | 소유권 강제 — `buildTaskWhere` 에 `user_id` 고정, 전 API·화면에 세션 확인과 소유 조건, 내보내기 DTO | ☑ 완료 | Claude |
| **A5** | 자동 검사 — 비밀번호 복잡도·아이디/닉네임 형식·소유자 조건·DTO 누출·지표 규칙·날짜 범위·복구·DB 시간대·계획 색상·날짜 입력 | ☑ 28건 통과 | Claude + Codex |
| **A6** | **DB 마이그레이션 적용** + 소유자 계정 가입 + 과제 6 자료 이전 (T07-C100) | ☑ 완료 | 사용자 + Codex |
| **A7** | 실제 동작 확인 및 **차단 증거 수집** — 설명서 4항 확인 1~5의 요청·응답 | ☑ 자동 검증(양방향 404·유출 0·상대 자료 불변) + Postman 화면 A10~A22. 나머지 요청별 화면은 사용자 판단으로 자동 검증 결과로 갈음 | Codex → Claude |
| **A8** | 배포, 시크릿 창에서 공개 선택 화면과 미로그인 자료 주소의 로그인 차단 확인 (T07-C01 / C03) | ☑ 공개 배포·PC 증거 완료, 첫 화면 형식은 사용자 요청으로 변경 | Codex |
| **A9** | 배포 직후 `DROP DEFAULT` — 임시 호환 장치 제거 | ☑ 완료 | Codex |
| **A10** | **5일 실제 사용** + 2일차 뒤·3일차 앞 계획 규칙 1개 변경 (T07-C07 ~ C15) | ☑ 2026-09-03 완료 — 8/29~9/2 완료·실행 기록 5건 입력, §2·§4 수치 채움 | 사용자 + Claude |
| **A11** | 화면 합계와 손 계산 대조, 최종 보고서, 비밀값 최종 스캔 | ☑ §5 대조표·T07-A17 촬영·제출 보고서 §4~§5 반영 완료. 비밀값 스캔 0건 | Claude + 사용자 |

## 3. 결정 기록

| 날짜 | 결정 | 근거 | 대안과 기각 이유 |
|---|---|---|---|
| 2026-09-01 | 인증을 **직접 구현(자체 DB 세션)** + `bcryptjs` 로 붙인다 | T07-C114 가 "로그아웃하면 이전 값이 안 통함"을 요구 → 서버가 세션을 폐기할 수 있어야 함 | Auth.js v5 → Credentials 를 쓰면 세션이 JWT 로 고정되어 폐기 불가. Supabase Auth → 로그아웃 후에도 액세스 토큰이 만료 전까지 유효 |
| 2026-09-01 | 비밀번호 해시는 **bcrypt(cost 12)**, `bcryptjs 3.0.3` | 계정마다 소금값을 자동 생성·포함 → T07-C104 자동 충족. 순수 JS 라 서버리스 네이티브 빌드 불필요 | scrypt/PBKDF2 → 소금·형식·비교를 직접 작성. argon2 → 네이티브 빌드 실패 위험 |
| 2026-09-01 | 세션 토큰은 **불투명 난수**, DB 에는 **SHA-256 해시만** 저장 | DB 유출 시에도 세션 도용 불가. 서명키가 아예 없어 T07-C113 의 위험 자체가 사라진다 | JWT → 폐기 불가 + 서명키 관리 부담 |
| 2026-09-01 | 남의 자료 거절은 **404** | `WHERE id=$1 AND user_id=$2` 가 0행 → 자연스럽게 404. 수정·삭제도 같은 WHERE 라 **거절 시 상대 자료가 바뀌지 않음**(T07-C122 자동 충족) | 403 → 자료의 존재를 알려 줌 |
| 2026-09-01 | 목록 차단은 `buildTaskWhere()` **한 곳**에서 | 목록·검색·거르기·집계가 모두 이 함수를 지남 | 각 라우트에서 개별 처리 → T07-C125 누수 위험 |
| 2026-09-01 | **과제 6 DB 를 그대로 공유**한다 (사용자 결정) | 과제 6과 과제 7이 한 저장소·한 배포로 이어지므로 자료가 갈라지면 안 됨 | 새 DB → 나중에 두 벌의 자료를 합쳐야 함 |
| 2026-09-01 | 소유자 컬럼에 **임시 `DEFAULT`** 를 걸고 4단계로 마이그레이션 | 인증판 배포 전까지 과제 6 배포가 `user_id` 없이 INSERT 해도 깨지지 않게 | 바로 `NOT NULL` → 과제 6 앱의 등록 기능이 즉시 깨짐 |
| 2026-09-01 | **아이디와 이메일을 분리**하고 **닉네임**을 따로 저장 (사용자 결정) | 로그인은 아이디로, 표시는 닉네임으로. 이메일은 비밀번호 찾기용으로 보관 | 이메일을 아이디로 사용 → 표시 이름이 이메일로 노출 |
| 2026-09-01 | 중복확인은 **입력 중 확인 + DB 유니크 인덱스** 두 겹 (사용자 결정) | 화면 확인은 편의이고 실제 보장은 DB. 확인과 제출 사이에 남이 먼저 가입하는 경우까지 막힌다 | 화면 확인만 → 동시 가입 시 중복 통과 |
| 2026-09-01 | 중복확인 API 는 **아이디·닉네임만** 확인해 준다 | 이메일까지 열어 주면 누가 가입했는지 캐낼 수 있다 | 이메일도 확인 → 가입자 존재 여부 유출 |
| 2026-09-01 | 비밀번호는 **10자 이상 + 대문자·소문자·숫자·특수문자 각 1자 이상** (사용자 요청) | 무차별 대입 방어가 없는 상태에서 추측 난이도를 올리는 유일한 수단 | 길이만 검사 → 흔한 단어 비밀번호가 그대로 통과 |
| 2026-09-01 | **MVC 계층 + DTO** 도입 (사용자 요청, 포트폴리오 관점) | Repository 의 모든 함수가 `user_id` 를 받게 하면 소유자 조건을 빠뜨릴 자리가 없어진다. DTO 화이트리스트는 `SELECT *` 사고를 막는다 | 계층 없이 유지 → 새 라우트 추가 시 소유자 조건 누락 가능 |
| 2026-09-01 | **기존 라우트는 계층 이동 없이 소유 조건만 넣는다** | 동작하는 과제 6 코드를 크게 흔들지 않으면서 차단을 먼저 확보한다. 새 인증 코드는 계층 구조를 지킨다 | 전 라우트를 Repository 로 옮기기 → 첫 구현에서 회귀 위험이 커진다. **남은 일로 5절에 적어 둔다** |

## 4. 작업 로그

| 날짜         | 작업자         | 내용                                                                                                                                                                                                |
| ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-01 | Claude Code | 과제 7 설계 확정, 문서 세트를 `docs/과제7/` 에 구축. 과제 6 문서는 그대로 보존                                                                                                                                              |
| 2026-09-01 | Claude Code | 과제 6 잔여 문서 수정과 보고서 파일명 정리를 `main` 에 커밋 (`f882da6`)                                                                                                                                                |
| 2026-09-01 | Claude Code | `bcryptjs 3.0.3` 설치. `schema_auth.sql` 작성 — `app_user`(아이디·닉네임·이메일 lower() 유니크 3개), `user_session`, 소유자 컬럼 4단계 마이그레이션                                                                             |
| 2026-09-01 | Claude Code | 인증 코어 구현 — `lib/domain/{rules,metric}.ts`, `lib/repository/{user,session}.ts`, `lib/service/auth.ts`, `lib/session.ts`, `lib/dto/{user,records}.ts`, `lib/cookie.ts`                              |
| 2026-09-01 | Claude Code | 화면·API 구현 — 로그인·가입·계정 화면, `/api/auth/{check,signup,login,logout,password}`, `/api/account`, `middleware.ts`                                                                                       |
| 2026-09-01 | Claude Code | 소유권 강제 — `lib/query.ts`·`lib/planner.ts` 를 `lib/domain/` 으로 이동, `buildTaskWhere` 첫 조건에 `user_id` 고정, API 8곳·화면 6곳에 세션 확인과 소유 조건 추가, 내보내기를 DTO 경유로 변경                                              |
| 2026-09-01 | Claude Code | 검사 추가 및 전체 통과 — `node --test` 16건, `tsc --noEmit`, `next build`, `npm audit`(취약점 0건). 기존 단언 1건은 새 시그니처에 맞춰 **소유자 인자 확인까지 강화**                                                                     |
| 2026-09-02 | Codex       | 기존 `plan-do-see-diary` 저장소에서 과제 6 승인본을 보존하며 과제 7을 이어가는 방침 확인. 루트 README에 과제 6/7 상태와 전용 SSOT·문서·증거 경로를 명확히 분리 표기                                                                                   |
| 2026-09-02 | Codex       | `schema_auth.sql` 확장 단계를 한 트랜잭션으로 실제 DB에 적용. `app_user`·`user_session`과 NULL 허용 `user_id` 3개 생성, 기존 자료 행 수 계획 2·할 일 5·돌아보기 1 보존 확인                                                                |
| 2026-09-02 | Codex       | 마이그레이션 실행 스크립트에 미적용/완료/부분 적용 사전 검사, 순차 조회, 연결·락 제한 시간, 적용 전후 행 수 검증 추가. 자동 검사 16건·타입·빌드·감사(0건), `/signup` HTTP 200 통과                                                                             |
| 2026-09-02 | 사용자 + Codex | 사용자가 소유자 계정을 직접 가입한 뒤 기존 계획 2·할 일 5·돌아보기 1을 그 계정에 귀속. 임시 기본값과 `user_id NOT NULL`을 적용하고 미귀속 0건 확인                                                                                                  |
| 2026-09-02 | Codex       | 일회성 계정 2개로 미로그인 401, 중복 가입 409, bcrypt cost 12·서로 다른 해시, 양방향 타 계정 읽기·수정·삭제 404, 목록 유출 0건, 로그아웃·비밀번호 변경 후 이전 세션 401을 검증하고 계정·자료를 즉시 삭제. 가린 결과를 `docs/과제7/evidence/auth-isolation-results.json`에 저장 |
| 2026-09-02 | Codex       | 홈의 과제 6 공개 안내를 과제 7 계정별 비공개 안내로 교체하고 `docs/과제7/인증_구현_설명서.md` ①~⑥ 작성                                                                                                                               |
| 2026-09-02 | Codex       | A7 최종 재검증 통과 — 인증 격리 스크립트, 자동 검사 16건, 타입 검사, 프로덕션 빌드, `npm audit --omit=dev`(취약점 0건). DB URL·비밀 토큰·브라우저용 환경변수 사용을 로컬·Git 이력에서 스캔해 실제 값 0건 확인                                                      |
| 2026-09-02 | Codex       | 로그인 정보가 없는 분리 브라우저에서 로컬 로그인 화면을 PC 1920×1080으로 촬영해 `docs/과제7/evidence/T07-C03-login-local-pc.png`에 저장. 실제 계정·자료·비밀번호는 포함하지 않음                                                                     |
| 2026-09-02 | Codex       | `c1aaf195f674cff68da02a818737c1ffca650fe8`을 `main`에 푸시하고 Vercel production에 직접 배포. 기본 주소 `/`·`/tasks`는 `/login`으로 307, `/login`·`/signup`은 200, 미로그인 `/api/tasks`는 401 확인                         |
| 2026-09-02 | Codex       | 배포 직후 `plan`·`task`·`review.user_id`의 임시 `DEFAULT`를 제거하고 세 컬럼 모두 `NOT NULL` + 기본값 없음 확인. 배포 주소에서도 일회성 양방향 인증 격리 검증을 통과하고 자료를 정리함                                                                  |
| 2026-09-02 | Codex       | 분리된 미로그인 브라우저에서 공개 로그인 화면을 PC 1920×1080으로 촬영해 `docs/과제7/evidence/T07-C03-login-production-pc.png`에 저장                                                                                             |
| 2026-09-02 | Codex       | A0~A9의 코드·문서·공개 PC 증거를 `911ad122358dab76b5fcf6f2fd9d8f7c03a10b2e`에 고정하고 README·요구사항 매핑에 40자리 HTTPS URL을 기록                                                                                        |
| 2026-09-02 | Codex       | A10 연습용 `docs/과제7/A10_5일_기록_더미.md` 생성. 실제 날짜·수치는 모두 사용자 입력 자리로 남기고 DB 자료와 제출 증거는 만들지 않음                                                                                                           |
| 2026-09-02 | Codex       | 배포 환경 자동 검증에 내보내기와 계정 삭제를 추가. 내 자료 JSON 200·타 계정 유출 0건·내부 필드 0건, 일회성 계정과 연결 계획·할 일 삭제 0건 및 이전 세션 401 확인                                                                                           |
| 2026-09-02 | Codex       | `docs/과제7/과제7_제출_보고서.md` 초안 작성. A0~A9 결과를 모두 채우고 실제 5일 날짜·수치·판단만 빈칸으로 유지. 자동 검사 16건·타입·빌드·감사(0건)·비밀값 스캔(0건) 재통과                                                                                   |
| 2026-09-02 | Codex       | 사용자 A10 시작 전 완료 상태를 `de6d96519a46cbec02226471060487d8574df2f8`에 고정. README·요구사항 매핑·제출 보고서에 40자리 HTTPS URL 반영                                                                                      |
| 2026-09-02 | Codex       | 플래너 일간·주간·월간에 네이티브 드래그 앤 드롭을 추가. 로그인 사용자 소유 조건이 있는 날짜 전용 API로 할 날짜만 변경하며 제목·시간·실행 기록은 유지                                                                                                          |
| 2026-09-02 | Codex       | 할 일 시작·마감 시각과 실행 기록 시작·종료 시각으로 예상·실제 분을 서버에서 자동 계산. DB에 `start_minute`·`end_minute`와 범위·쌍 제약을 무중단 확장하고 기존 할 일 6건 보존, 재실행 안전성 확인                                                                   |
| 2026-09-02 | Codex       | 날짜 이동 소유권·서버 시간 계산 검사를 포함해 자동 검사 20건, 타입 검사, 프로덕션 빌드 통과. 일회성 계정으로 내 날짜 이동·원복 200, 타 계정 날짜 이동 404를 확인하고 전부 정리                                                                                      |
| 2026-09-02 | Codex       | PC 1294px 주간 플래너에서 드래그 가능 할 일 6개·날짜 드롭 영역 7개·가로 넘침 없음 확인. `docs/과제7/evidence/T07-planner-drag-pc.png`에 증거 화면 저장                                                                                   |
| 2026-09-02 | Codex       | `28a0a0b1ee580fa98e0fcb8459e9c90ae530f76d`를 `main`에 푸시하고 Vercel production에 배포. 공개 주소에서 내 날짜 이동·원복 200, 타 계정 날짜 이동 양방향 404, 인증·내보내기·계정 삭제 검증 통과. `/`·`/tasks` 미로그인 307 → `/login` 확인              |
| 2026-09-02 | Codex       | 날짜 이동·시간 자동 계산의 전체 소스·문서·배포 검증 상태를 `4462925ecf7d38b844c356c010e41b3f177b613e`에 고정하고 README·제출 보고서에 40자리 HTTPS URL 기록                                                                              |
| 2026-09-02 | Codex       | 공개 시작 화면을 로그인·회원가입 선택형으로 변경하고 계정 찾기, 비밀번호 보기, 닉네임 헤더, 버튼 즉시 처리 상태를 구현. 기존 `test` 계정의 지정 비밀번호 일치와 계획 1·할 일 5 보존 확인                                                                                 |
| 2026-09-02 | Codex       | 할 일의 계획 연결을 선택형으로 변경하고 시작일·마감일 범위 추가. 기존 할 일 11건의 시작일을 마감일로 안전하게 백필하고 행 수 보존·마이그레이션 재실행 확인                                                                                                        |
| 2026-09-02 | Codex       | 날짜 입력칸 전체 클릭, 시작·마감 시각 자동 분 계산, PC 입력 항목과 비밀번호 보기 확인. 자동 검사 24건·타입·빌드·감사(0건), 로컬 인증 격리·계획 없는 할 일·아이디 찾기 통과                                                                                        |
| 2026-09-02 | Codex       | `7413db2`를 `main`에 푸시하고 Vercel production에 배포. 공개 선택 화면 200, 자료 화면 307→`/login`, 복구 화면 200, `test` 로그인 200, 계획 없는 할 일·아이디 찾기·격리 자동 검증 통과                                                          |
| 2026-09-02 | Codex       | 공개 선택 화면을 PC 1294px에서 확인해 `docs/과제7/evidence/T07-public-entry-pc.png`에 저장                                                                                                                         |
| 2026-09-02 | Codex       | 사용성 개선의 전체 소스·문서·프로덕션 검증 상태를 `7d2a3eba0b6904dcaf76b6e8a82e81192289f936`에 고정하고 README·제출 보고서에 40자리 HTTPS URL 기록                                                                                    |
| 2026-09-02 | Codex       | 기존 `timestamptz` 값은 변경하지 않고 앱 DB 연결과 데이터베이스 새 연결의 기본 표시 시간대를 `Asia/Seoul(+09:00)`로 통일. 재실행 가능한 설정 명령과 자동 검사를 추가하고 실제 DB에서 시간대·UTC 오프셋을 확인                                                         |
| 2026-09-02 | Codex       | 플래너에서 할 일을 다른 날짜로 옮긴 뒤 `YYYY-MM-DD로 옮겼습니다.` 문구가 남지 않도록 성공 안내를 제거. 이동된 카드 자체로 결과를 보여 주고 실패 안내는 유지                                                                                                  |
| 2026-09-02 | Codex       | 계획 ID 기반 8색 고정 팔레트를 추가. 진행 계획 칩·계획 카드·플래너 할 일·전체 할 일의 계획 이름이 같은 색을 공유하고, 계획 없는 할 일은 중립색으로 구분                                                                                                      |
| 2026-09-02 | Codex       | 날짜 입력의 보이는 박스보다 감싼 제목 영역까지 달력 클릭 범위가 넓어지던 문제 수정. 포인터가 입력 박스 안에서 시작한 경우에만 달력을 열도록 제한                                                                                                               |
| 2026-09-02 | Codex       | 과제 6 승인 문서는 역사 스냅샷으로 보존하고 README와 과제 7 문서 전체를 현재 27건 검사·서울 DB 표시·계획별 색상·날짜 클릭 범위·최근 고정 소스·배포 상태에 맞춰 최신화                                                                                           |
| 2026-09-02 | Codex       | 최신화한 전체 소스·README·과제 7 문서 상태를 `db812fcd548f19cc4e4da82ea06063f40e21ee87`에 고정하고 주요 제출 문서에 40자리 HTTPS URL 반영                                                                                        |
| 2026-09-02 | Codex       | 공개 진입·로그인·가입·복구와 로그인 뒤 일간·주간·월간 플래너·계획·할 일·돌아보기·계정 화면을 PC 1440×900 환경에서 촬영. 전환 중 로딩 화면이 잡힌 파일은 안정화 후 재촬영                                                                                          |
| 2026-09-02 | Codex       | bcrypt·세션 폐기·양방향 소유권 차단·내보내기·계정 삭제·27개 자동 검사 결과를 비밀값 없는 화면으로 만들고 `docs/과제7/evidence/README.md`에서 T07-E01~E17과 요구사항을 연결. C07·C10~C12·C132는 실제 사용 전이라 증거를 만들지 않음                                    |
| 2026-09-02 | Codex       | PC 스크린샷 17장·증거 색인·요구사항 연결을 `23926e7a7b84a4ed0043fc4256af6f3573b67561`에 고정. README와 매핑 문서에 40자리 HTTPS 고정 URL 추가                                                                                    |
| 2026-09-03 | Codex       | 자체 점검 7줄을 요약 카드가 아닌 실제 앱·GitHub 원본 화면에 다시 연결. 미로그인 `/tasks`→로그인, 공개 인증 격리 JSON, 설명서 ①~⑥, 실제 DB bcrypt 모양과 비밀값 실제 값 대조 결과를 T07-A01~A04로 촬영                                                         |
| 2026-09-03 | Codex       | 자체 점검 1·2·3·4·5·7은 실제 화면 증거 완료. 6은 날짜별 할 일 화면과 수정 이력이 있어도 실제 5일 사용이 끝나지 않았으므로 통과 증거로 표시하지 않고 A10 대기 유지                                                                                            |
| 2026-09-03 | Codex       | 사용자 지적에 따라 DB를 재대조. `test` 계정에 활성 할 일 5건·서로 다른 마감일 5개(8/31~9/4)가 실제로 있음을 확인하고 자체 점검 6을 미완료가 아닌 부분 충족으로 정정. 완료일은 1개(모두 9/2), 실행일·실행 기록은 0개이고 규칙 변경 시점 조건은 미확인이라 최종 통과는 유지하지 않음                     |
| 2026-09-03 | 사용자 + Codex | 계획 규칙을 각 할 일 60분 이하에서 90분 이하로 변경한 실제 이유를 사용자 확인. DB 수정 이력의 서울 시각 `2026-09-03 01:23:55`와 이유를 5일 기록 문서에 반영하고 C10·C11 완료 처리. 1·2일차 날짜 지칭 C12는 미확정 유지                                                 |
| 2026-09-03 | 사용자 + Codex | 사용자가 규칙 변경의 원래 시각이 `2026-08-31 10:30:55`임을 확인. DB의 할 일 생성일 8/29·8/30·8/31·9/1·9/2와 변경 뒤 3일차 생성 시각 8/31 15:30:23을 대조해 C07·C10·C12 완료 처리. 앞선 사후 변경 추정은 철회                                           |
| 2026-09-03 | Codex       | 실제 5일 DB 시각·60분→90분 규칙 변경·사용자 확인 이유를 한 화면에 담아 `T07-A05-five-day-rule-change-actual-pc.jpg`로 저장하고 증거 색인에서 C07·C10~C12에 연결                                                                          |
| 2026-09-03 | Codex       | 카드 1~5의 모든 통과 기준을 기존 실제 PC 앱·GitHub 검증 원본 스크린샷에 1:1 연결한 `docs/과제7/evidence/카드1-5_통과기준_스크린샷.md`를 추가. 카드 1~4는 완료, 카드 5는 C132 손계산 대조 화면 1건만 대기로 명시                                                   |
| 2026-09-03 | Codex       | 사용자의 실제 DB 화면 사용 승인에 따라 Vercel Supabase 데이터 편집기에서 `plan_revision` 규칙 변경 행과 `task` 5일 생성 행을 PC 화면으로 촬영해 T07-A06·A07로 저장. 이메일·전체 비밀번호 해시·세션 해시가 노출되는 표는 촬영에서 제외                                     |
| 2026-09-03 | Codex       | 다른 증거도 DB 우선으로 재정리. 읽기 전용 쿼리로 bcrypt 형식·길이·평문 아님, 세션 SHA-256 길이·만료, 소유자 미귀속 0건, 서울 5일, 수정 시각을 T07-A08로 촬영하고 실제 8개 표 관계도를 T07-A09로 저장. API 401·404와 Git 비밀값 스캔은 DB만으로 증명할 수 없어 기존 실제 화면 유지         |
| 2026-09-03 | Codex       | Postman에서 기존 테스트 계정으로 미로그인 401, 아이디 중복, 로그인 200, 로그아웃 200, 로그아웃 뒤 401, 없는 아이디·틀린 비밀번호의 동일 401을 재현해 T07-A10~A16으로 촬영. 요청 본문은 필요한 화면에 포함하되 실제 비밀번호·쿠키·토큰은 가렸고 새 계정은 만들지 않음                          |
| 2026-09-03 | Codex       | `GET /api/tasks`가 DB 행 전체를 반환하던 DTO 누출을 `publicTask`로 수정하고, 같은 유형이 있던 계획 목록·상세·수정 이력·할 일 생성/상세/수정·실행 기록·돌아보기 응답까지 기존 DTO로 일괄 보강. 자동 검사 28건·타입 검사·프로덕션 빌드·감사(취약점 0건) 통과                            |
| 2026-09-03 | Codex       | DTO 수정본 `67698889bfa7afbb3be1add85e39df65791c29b7`을 `main`에 푸시하고 Vercel Git 연결을 복구해 Production 배포 Ready 확인                                                                                        |
| 2026-09-03 | Codex       | 실제 Postman에서 DTO 적용 목록 200(T07-A18), 증거 계정 가입 201(A19), 해당 계정의 계획·할 일 생성 201(A20·A21), 해당 계정 목록에 본인 할 일만 1건 표시(A22)를 촬영. 증거 계정과 계획·할 일은 사용자 지시에 따라 **삭제하지 않고 보존**                                 |
| 2026-09-03 | Codex       | 사용자 요청으로 추가 검증을 중단하고 Claude 인계 준비. 촬영되지 않은 양방향 읽기·수정·삭제 Postman 화면과 C132만 미완료로 남김                                                                                                                 |
| 2026-09-03 | Claude Code | 최종 검증 재실행 — `node --test` 28건 통과, `tsc --noEmit` 오류 0, `next build` 성공, `npm audit --omit=dev` 취약점 0건. 읽기 전용 DB 조회로 보존된 두 계정의 실제 id만 확인(비밀번호는 조회하지 않음)하고 임시 스크립트는 실행 직후 삭제 — `test`(id 23, plan 21, task 23~27), `evidence_user_0903`(id 36, plan 28, task 36) |
| 2026-09-03 | Claude Code | **전면 재검증(Reviewer)** — ① 자동 검사 4종 재통과: `node --test` 28건, `tsc --noEmit` 오류 0, `next build` 성공, `npm audit --omit=dev` 취약점 0건. ② 코드 감사: 자료 API 9개 전부 `getSessionUser` 경유, 응답이 DB 행 그대로 나가는 곳 0건(의심됐던 `/api/review` GET·`/api/tasks/[id]/date` PATCH 는 명시 컬럼만 반환해 안전), `buildTaskWhere` 첫 조건 `user_id = $1` 고정, URL·헤더·본문에서 사용자 ID 를 읽는 코드 0건. ③ 비밀값: 추적 중인 `.env`/키 파일 0건, 소스·문서·**Git 전체 이력** 패턴 스캔 0건. ④ 증거 대조: 문서가 참조하지만 없는 파일은 `T07-A17`(C132 대기분) 1건뿐. ⑤ 고정 commit 8개 실재 확인, 과제 6 `356a460` 이 `main` 의 조상임을 `git merge-base` 로 확인(C78) |
| 2026-09-03 | Claude Code | 공개 배포 **미로그인** 재확인 — `/`·`/login`·`/signup`·`/recover` 200, `/tasks`·`/plans`·`/review` 307→`/login`, `/api/tasks`·`/api/plans`·`/api/export` 401. 로그인하지 않고 확인 가능한 범위만 수행 |
| 2026-09-03 | Claude Code | 읽기 전용 `scripts/five-day-report.mjs 23` 재실행으로 §7 실측 재확인 — 완료 기록은 9/3 1건(실행 기록 없음), 실행 기록은 9/2 1건(task 27, 2분). 날짜별 지표 수치는 여전히 DB 에 없음 |
| 2026-09-03 | Claude Code | **문서 모순 수정** — 자체 점검 6번이 `00_과제_요구사항_매핑.md` 안에서 ☑(증거 표)와 ☐(완주 표)로 엇갈리던 것을 실측에 맞춰 양쪽 모두 ◐ 로 통일하고 근거 명시. `evidence/README.md` 6번 행도 같은 기준으로 정정. 자체 점검 1번은 공개 배포 재확인 결과로, 4번은 "자동 검증은 ☑·양방향 화면은 ◐" 로 구분 표기 |
| 2026-09-03 | Claude Code | 공개 배포 화면을 **헤드리스 Chrome**(1440×900, 로그인 정보 없는 새 프로필)으로 촬영 — 시작·로그인·가입·복구 4장. 헤드리스는 주소 표시줄이 없어 "`/tasks` 로 갔더니 `/login` 이 나왔다"는 **이동 자체는 증명하지 못한다**. 기존 `T07-E01~E04`·`T07-C03-login-production-pc.png` 와 중복이라 증거 폴더에는 넣지 않고 사용자 판단으로 남김 |
| 2026-09-03 | Claude Code | ⚠️ **이 PC 에서는 화면 캡처 방식으로 증거를 만들지 않는다.** 주소 표시줄이 있는 실제 창을 찍으려고 창 캡처를 시도했더니 사용자의 **개인 Chrome 창**(개인 탭·북마크·OAuth 인가 코드가 들어간 주소)이 두 차례 찍혔다. 두 파일과 캡처 스크립트를 즉시 삭제했고 증거 폴더·Git 에는 들어가지 않았다. 이후 촬영은 사용자가 직접 하거나, 화면 전체가 아닌 헤드리스 렌더링만 쓴다 |
| 2026-09-03 | 사용자 + Claude Code | **A10 5일 수치 확정.** 사용자가 기간을 8/29~9/2로, 실제 걸린 시간을 5일 모두 **4분**으로 확인. `scripts/record-five-day.mjs` 로 완료 표시 5건·실행 기록 5건을 한 트랜잭션에 입력(완료 1→5건, 실행 1→5건). 시작 시각은 지어내지 않고 각 할 일의 실제 `created_at`(서울)을 그대로 사용. 사용자 승인 아래 기존 task 23 완료 시각을 9/3→8/29로 옮기고 task 27 실행 기록을 2분→4분으로 맞춤 |
| 2026-09-03 | Claude Code | `five-day-report.mjs` **날짜 정렬 버그 수정** — `String(day).slice(0,10)` 이 `"Mon Aug 31"` 을 만들어 요일 이름 알파벳순(8/31→8/29→8/30→9/1→9/2)으로 정렬되던 것을 `to_char(...,'YYYY-MM-DD')` 로 바꿔 날짜순으로 교정. 전후 비교의 구간 구분에 영향 있는 버그였다 |
| 2026-09-03 | Claude Code | `5일_사용기록.md` §2 다섯 표·§4 전후 비교·§5 손계산 대조를 실제 집계로 채움. 1~5일차 오차율 33.3 / -93.5 / 0.0 / 0.0 / -50.0 %, 합계 예상 81분·실제 20분, 평균 -22.0%. 변경 전 -30.1% → 변경 후 -16.7% |
| 2026-09-03 | Claude Code | ⚠️ **§5 자체 검증에서 오류 발견·수정.** 처음엔 화면 기간을 8/29~9/2로 적었으나, 돌아보기 화면은 **완료일이 아니라 마감일**로 거른다(`buildTaskWhere` 의 `due_date`). 5일치 마감일이 8/31~9/3이라 9/2까지만 잡으면 4건·예상 77분·실제 16분만 나온다. 기간을 **8/29~9/3**으로 정정. 또 화면에는 **평균 오차율 항목이 없어**(예상·실제·차이 3개뿐, `app/review/page.tsx:50-52`) 평균은 손계산만 기록하도록 표를 고쳤다 |
| 2026-09-03 | 사용자 + Claude Code | **T07-A17 촬영 완료 (C132).** 사용자가 Claude in Chrome 확장을 연결해 기존 로그인 세션을 열어 주어, 비밀번호 입력 없이 공개 배포 `/review` 화면을 촬영했다. 기간 2026.08.29–2026.09.04 에서 계획 5·완료 5·예상 81분·실제 20분·차이 -61분이 §5 손계산과 일치 |
| 2026-09-03 | Claude Code | ⚠️ **촬영 도중 자료 변경 감지.** 첫 촬영본이 계획 4·예상 15분·실제 16분으로 나와 문서(81/20/-61)와 어긋났다. DB 재조회로 사용자가 task 24 예상을 62→4분, task 27 마감일을 9/2→9/4 로 바꾼 것을 확인하고 **잘못된 증거 파일을 즉시 삭제**했다 |
| 2026-09-03 | 사용자 + Claude Code | 사용자 지시로 task 24 를 62분으로 되돌림. `task_schedule_pair` 제약이 `estimate_minutes = end_minute - start_minute` 을 강제하므로 예상 시간만 바꾸면 위반이라, 시작 시각(984분)은 두고 마감을 1046분으로 함께 갱신. 소유자 조건을 건 UPDATE 1행, 다른 칸은 건드리지 않음. 합계가 예상 81분·실제 20분·평균 -22.0% 로 복구됨 |
| 2026-09-03 | Claude Code | task 27 마감일이 9/4 로 바뀐 상태가 유지되어 §5 화면 기간을 8/29~9/3 → **8/29~9/4** 로 정정. 9/3 까지만 잡으면 4건·예상 73분·실제 16분만 잡힌다는 근거를 함께 기록 |
| 2026-09-03 | Claude Code | 제출 보고서 §5 를 실제 결과로 교체 — 5일 날짜, 규칙 변경 시각·이유, 전후 평균 오차율(-30.1% → -16.7%), 합계 81/20/-61, 손계산 일치와 T07-A17 연결. 개선의 대부분이 2일차 한 건에서 나온 점과 화면 기간이 9/4 인 이유를 함께 적었다 |
| 2026-09-03 | 사용자 + Claude Code | 양방향 격리를 **자동 검증 결과로 갈음**하기로 확정(사용자 판단). `auth-isolation-results.json` 의 `isolation` 두 방향에 읽기·수정·삭제·날짜 이동 404, `target_unchanged` true, `leaked_rows` 0 이 2026-09-02 공개 배포 대상으로 기록되어 있음을 확인. 보고서 §4·매핑·증거 색인에 근거를 명시하고 A7 을 완료 처리 |
| 2026-09-03 | Claude Code | 사용자가 촬영만 하면 되도록 `docs/과제7/과제7_양방향격리.postman_collection.json` 작성 — 양방향 404·목록 격리 요청 12개에 기대 상태 코드와 "남의 id 없음"·"내부 필드 없음" 자동 검사를 붙였다. 비밀번호는 secret 변수 2칸을 **빈 값**으로 두어 파일에 원문이 들어가지 않는다 |
| 2026-09-03 | Claude Code | Postman·Supabase 대시보드 로그인이 필요한 나머지 증거(양방향 GET·PATCH·DELETE 404 촬영, 거절 전후 DB 화면, C132 손계산)는 AI가 어떤 계정 비밀번호도 로그인 필드에 대신 입력할 수 없어 직접 수행할 수 없음. 실행 순서를 5·6절에 구체적으로 정리해 사용자에게 인계 |

## 5. 남은 문제 / 확인 필요

- **기존 라우트는 아직 Repository 계층으로 옮기지 않았다.** 소유 조건은 각 라우트의 SQL 에 직접 들어가 있다. 새 인증 코드만 계층 구조를 지킨다. 옮길 때 회귀가 없도록 검사를 먼저 붙인다.
- DTO 누출은 전체 자료 API에서 수정했고 A18의 실제 공개 배포 응답으로 `user_id`·`deleted_at` 미노출을 확인했다.
- 실제 화면 제출 증거는 **공개 배포 앱·Postman·실제 DB 화면만** 사용한다. GitHub 문서/JSON 화면과 정적 요약 화면은 참고 자료로만 보존하고 제출 증거에서 제외한다.
- 증거 계정(id 36), 계획(id 28), 할 일(id 36)은 이후 양방향 격리 확인을 위해 보존한다. 비밀번호 원문은 문서·화면에 남기지 않는다.
- 카드 5의 C132는 실제 분·오차율이 입력된 뒤 돌아보기 화면 합계·평균과 손계산을 한 화면에서 대조해 `T07-A17-five-day-totals-hand-check-pc.jpg`로 촬영해야 한다.
- **AI는 로그인이 필요한 나머지 단계를 대신 수행할 수 없다.** Postman·Supabase·배포 앱에 계정 비밀번호를 입력하는 것은 본인 소유 서비스라도 AI 안전 규칙상 금지되어 있다. 아래 6절은 사용자가 직접 진행하는 순서다.

## 6. 남은 단계 — 사용자가 직접 진행 (로그인 필요, AI 대행 불가)

**2026-09-03 Claude Code 확인**: `npm test`(28건 통과) · `npm run typecheck`(오류 0) · `npm run build`(성공) · `npm audit --omit=dev`(취약점 0건)를 재실행해 모두 통과했다. 여기에 더해 소유권·DTO·비밀값 코드 감사, 공개 배포 미로그인 응답 재확인, 증거 파일 대조, 문서 모순 수정까지 마쳤다(§4 로그 참고). 아래는 로그인이 필요해 AI가 대신 할 수 없는 나머지 항목이며, 읽기 전용 DB 조회로 실제 id만 미리 확인해 두었다(비밀번호는 조회하지 않음).

> **푸시 대기**: 로컬 `main` 이 `origin/main` 보다 커밋 2개 앞서 있다(문서·읽기 전용 스크립트·`.gitignore` 만 포함, 앱 코드 변경 없음 → 배포 결과에 영향 없음). 여기에 오늘 문서 수정분이 더해진다. 사용자가 푸시를 승인하면 함께 올린다.

**확인된 실제 id (비밀번호 아님)**

| 계정 | user id | login_id | plan id | task id |
|---|---|---|---|---|
| 소유자(과제 6 이전 자료) | 23 | `test` | 21 | 23, 24, 25, 26, 27 |
| 증거 계정(보존) | 36 | `evidence_user_0903` | 28 | 36 |

### 3. Postman 양방향 404 촬영

배포 주소: `https://plan-do-see-diary.vercel.app`

> **바로 쓰는 모음 파일**: `docs/과제7/과제7_양방향격리.postman_collection.json` 을 Postman 에서 Import 하면 아래 1~5번이 요청 12개로 이미 만들어져 있다.
> 모음 > Variables 의 `pw_test`·`pw_evidence` 두 칸(secret 형식이라 ●●● 로 표시)에 비밀번호만 넣고 위에서부터 실행한다.
> 각 요청에 기대 상태 코드와 "목록에 남의 id 가 없다" 검사가 붙어 있어 Postman 이 통과·실패를 자동 표시한다. 파일에는 비밀번호가 들어 있지 않다.

1. **`test` 계정으로 로그인** → `POST /api/auth/login` body `{"id":"test","password":"<본인 비밀번호>"}` → 응답 쿠키를 Postman이 저장하도록 같은 요청 창에서 진행.
2. 로그인한 `test` 세션으로 **증거 계정의 할 일(36)** 에 요청 3개를 보내고 모두 404인지 촬영: `GET /api/tasks/36`, `PATCH /api/tasks/36`(body 아무 필드나, 예 `{"title":"x"}`), `DELETE /api/tasks/36`.
3. `GET /api/tasks` 목록 응답에 `id: 36`이 섞이지 않는지 촬영.
4. **로그아웃 후 `evidence_user_0903` 계정으로 로그인** → 같은 방식으로 **`test`의 할 일(23~27 중 1건, 예 23)** 에 `GET`·`PATCH`·`DELETE`를 보내 404 확인, `GET /api/tasks` 목록에 23~27이 섞이지 않는지 촬영.
5. 요청 본문은 그대로 보이게 두되, 비밀번호·`Cookie` 헤더·세션 토큰 값은 캡처 전 가린다.

### 4. 거절 전후 DB 대조

Supabase 대시보드(Table Editor)에서 위 시도 **전후**로 `task` 표의 id 23(또는 26/27 중 아직 손대지 않은 것)과 id 36 두 행의 `title`·`updated_at`(또는 트리거 기록)·`deleted_at`이 그대로인지 화면으로 대조한다.

### 5. C132 손계산 대조

`docs/과제7/5일_사용기록.md`가 아직 `(n)` 자리 그대로다 — 실제 5일 사용의 완료 건수·예상 분·실제 분이 입력된 뒤에만 가능하다. 값을 채운 뒤 돌아보기 화면 합계·평균과 손계산을 한 화면에 담아 `T07-A17-five-day-totals-hand-check-pc.jpg`로 저장하고, 이 문서 §5 표를 채운다.

### 완료 후

새 스크린샷을 `docs/과제7/evidence/`에 추가하고 `evidence/README.md`의 표·"Claude 인계" 절을 갱신한 뒤, 이 체크리스트와 함께 커밋·푸시한다.

---

## 7. 2026-09-03 Claude Code 실측 — 5일 기록 수치를 채울 수 없는 이유

`scripts/five-day-report.mjs` 와 읽기 전용 조회로 **공유 DB 의 실제 자료**를 확인했다.
아래는 조회 결과 그대로이며, 비밀번호·해시·세션 값은 조회하지 않았다.

| 계정 | user id | 계획 | 할 일 | **완료** | **실행 기록** | 돌아보기 |
|---|---|---|---|---|---|---|
| `test` (소유자) | 23 | 1 | 5 | **1** | **1** | 0 |
| `evidence_user_0903` | 36 | 1 | 1 | 0 | 0 | 0 |

- 완료 기록은 **2026-09-03 하루에 1건**뿐이다 (할 일 23 `test1`, 예상 3분, 실행 기록 없음 → 미기록).
- 실행 기록은 **2026-09-02 하루에 1건**뿐이다 (할 일 27, 2분).
- 할 일 5건의 **마감일**은 08-31 ~ 09-03 으로 흩어져 있지만, 마감일은 지표 대상이 아니다.
  지표는 **그날 완료 표시된 할 일**을 쓴다(1절 계산 규칙).

**따라서 T07-C07(서로 다른 실제 날짜 정확히 5일의 기록)과 C132(화면 합계와 손계산 대조)는 현재 충족되지 않는다.**
`docs/과제7/5일_사용기록.md` 의 날짜 5칸과 규칙 변경 시각은 채워져 있으나, 그 날짜들에 해당하는 완료·실행 기록이 DB 에 없다.

### 채우려면 필요한 것 (사용자만 가능)

1. 5일(2026-08-29 · 08-30 · 08-31 · 09-01 · 09-02) 각각에 **완료 표시**와 **실행 기록(실제 분)** 이 있어야 한다.
2. 이미 지난 날짜의 완료 시각은 화면에서 만들 수 없다. 실제로 그날 쓰지 않았다면 **날짜 5칸을 앞으로의 5일로 다시 잡는 편이 정직하다.**
3. 값이 들어오면 `node scripts/five-day-report.mjs 23` 을 실행해 날짜별 완료 건수·예상 분·실제 분·오차율·미기록을 그대로 받아 `5일_사용기록.md` 에 옮긴다.

**AI 는 이 수치를 만들어 넣지 않는다.** 실제 자료를 지어내지 않는다는 규칙(`CLAUDE.md` 9번)에 따른다.

## 8. 2026-09-03 Claude Code 추가 증거

`docs/과제7/evidence/T07-미로그인차단-요청응답-2026-09-03.md` — 공개 배포에 **쿠키 없이** 보낸 요청과 응답 원문 7건.
로그인 없이 확보할 수 있는 항목만 담았다: 미로그인 401·307, 아이디 없음과 비밀번호 틀림의 동일 문구(T07-C99),
같은 아이디 재가입 409(T07-C98), 비밀번호 규칙 400, 중복확인이 사용 가능 여부만 응답.
실제 비밀번호는 쓰지 않았고 계정을 만들지 않았다.
