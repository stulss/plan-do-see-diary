# 작업내역 체크리스트 — 과제 7 「잠긴 플랜두씨 다이어리」 (SSOT)

이 파일 하나만 읽으면 현재 상태를 알 수 있어야 한다. 모든 AI와 사용자가 공용으로 누적 기록한다.

## 1. 현재 상태

- **과제**: 과제 7 — 과제 6 다이어리에 가입·로그인을 붙여 내 자료를 나만 보게 만들기
- **단계**: **A0 완료** (설계·문서 세트 구축). 다음은 A1(스키마 적용)
- **작업 폴더**: `C:\Users\stuls\Desktop\Agent\plan-do-see-diary_to7` — **이 폴더에서만 작업한다**
- **저장소**: `https://github.com/stulss/plan-do-see-diary` 의 **`plan-do-see-diary_to7` 브랜치** → 완성 후 **PR 로 `main` 병합**
- **이어받은 과제 6 고정 commit**: `356a46034e94084caec3e17eaaf2ab8d98cef7ce` (조상 포함 확인 완료)
- **데이터베이스**: **과제 6 것을 그대로 공유**한다 (PR 병합 시 자료가 갈라지지 않게)
- **과제 6 상태**: **최종 승인 통과.** 제출본 보존 제약은 풀렸다
- **배포**: 아직 없음
- **다음 할 일**: Codex — A1~A5 구현 (6절 프롬프트)

### 로그인 방법 (확정)

| 방법 | 입력 | 비고 |
|---|---|---|
| 일반 | **아이디 · 비밀번호** | 가입 시 아이디 · 닉네임 · 이메일 · 비밀번호 4칸 |
| 구글 | OIDC | `openid-client` |
| 카카오 | OIDC | `openid-client`, 앱 설정에서 OIDC 활성화 필요 |

### ⛔ 절대 지킬 것

| 금지 | 이유 |
|---|---|
| `main` 브랜치에 직접 커밋 | PR 로만 합친다 |
| `git init` 으로 새 저장소 만들기 | T07-C78(과제 6 고정 커밋이 조상) 탈락 |
| `user_id` 를 처음부터 `NOT NULL` 로 추가 | 같은 DB 를 쓰는 과제 6 배포의 등록 기능이 깨진다 (기획 4-3) |
| 비밀번호·토큰·클라이언트 시크릿 원문을 코드·문서·로그에 남기기 | T07-C46 / C131 |
| 사용자 ID 를 URL·헤더·요청 본문에서 읽기 | T07-C123. 사용자는 **세션에서만** 확정한다 |
| 중복확인을 화면에서만 하고 DB 유니크 인덱스를 빼기 | 동시 가입 시 중복이 그대로 들어간다 |
| DB 행을 그대로 응답에 싣기 (`SELECT *` 를 그대로 반환) | 새 컬럼이 자동으로 응답에 실려 나간다. 반드시 `lib/dto/` 를 거친다 |

## 2. 진행 체크리스트

| 단계 | 내용 | 상태 | 담당 |
|---|---|---|---|
| **A0** | 과제 6 클론 → `plan-do-see-diary_to7` 브랜치, 과제 6 문서 전량 제거, 과제 7 표준 문서 세트 구축, 인증 설계 확정(소셜 로그인·비밀번호 규칙·중복확인·출력 DTO 포함) | ☑ 완료 | Claude |
| **A1** | `schema_auth.sql` 작성 및 **과제 6 DB 에 적용** — 표 3개 추가 + 소유자 컬럼 4단계 마이그레이션 | ☐ | Codex |
| **A2** | 인증 구현 — `lib/service/auth.ts`, `lib/session.ts`, `middleware.ts`, 가입·로그인·로그아웃·중복확인 화면·API | ☐ | Codex |
| **A3** | 소셜 로그인 — `lib/service/oauth.ts`, `lib/repository/identity.ts`, 구글·카카오 OIDC 라우트, 계정 연결 규칙 | ☐ | Codex + 사용자(앱 등록) |
| **A4** | 소유권 강제 + 출력 DTO — `buildTaskWhere()` 에 `user_id` 고정, 모든 API·페이지에 `requireUser()`, `lib/dto/` 경유 응답 | ☐ | Codex |
| **A5** | 계정 화면 — 닉네임 변경, 비밀번호 변경(전 세션 폐기), 내보내기 소유 제한, 계정 삭제 | ☐ | Codex |
| **A6** | 자동 검사 + **차단 증거 수집** — 설명서 4항 확인 1~5의 실제 요청·응답 | ☐ | Codex + agy |
| **A7** | 배포, 시크릿 창에서 첫 화면이 로그인 화면인지 확인 (T07-C01 / C03) | ☐ | Codex + 사용자 |
| **A8** | **PR 로 `main` 병합** + `DROP DEFAULT` — 과제 6 주소도 인증판으로 전환 | ☐ | 사용자 |
| **A9** | **5일 실제 사용** + 2일차 뒤·3일차 앞 계획 규칙 1개 변경 (T07-C07 ~ C15) | ☐ | 사용자 |
| **A10** | 화면 합계와 손 계산 대조, 최종 보고서, 비밀값 최종 스캔 | ☐ | Codex + 사용자 |

## 3. 결정 기록

| 날짜 | 결정 | 근거 | 대안과 기각 이유 |
|---|---|---|---|
| 2026-09-01 | 인증을 **직접 구현(자체 DB 세션)** + `bcryptjs` + `openid-client` 조합으로 붙인다 | T07-C114 가 "로그아웃하면 이전 값이 안 통함"을 요구 → 서버가 세션을 폐기할 수 있어야 함 | Auth.js v5 → Credentials 를 함께 쓰면 세션이 JWT 로 고정되어 폐기 불가. Supabase Auth → 로그아웃 후에도 액세스 토큰이 만료 전까지 유효 |
| 2026-09-01 | 비밀번호 해시는 **bcrypt(cost 12)**, `bcryptjs` 사용 | 계정마다 소금값을 자동 생성·포함 → T07-C104 자동 충족. 순수 JS 라 서버리스 네이티브 빌드 불필요 | scrypt/PBKDF2 → 소금·형식·비교를 직접 작성. argon2 → 네이티브 빌드 실패 위험. SHA-256 1회 → 소금 없음, 너무 빠름 |
| 2026-09-01 | 세션 토큰은 **불투명 난수**, DB 에는 **SHA-256 해시만** 저장 | DB 유출 시에도 세션 도용 불가. 세션용 서명키가 아예 없어진다 | JWT → 폐기 불가 + 서명키 관리 부담 |
| 2026-09-01 | 남의 자료 거절은 **404** | `WHERE id=$1 AND user_id=$2` 가 0행 → 자연스럽게 404. 수정·삭제도 같은 WHERE 라 **거절 시 상대 자료가 바뀌지 않음**(T07-C122 자동 충족) | 403 → 자료의 존재를 알려 줌. 소유자 조회 후 판단 → 단계가 늘고 순서 실수 여지 |
| 2026-09-01 | 목록 차단은 `buildTaskWhere()` **한 곳**에서 | 목록·검색·거르기·집계가 모두 이 함수를 지남 | 각 라우트에서 개별 처리 → T07-C125 누수 위험 |
| 2026-09-01 | 같은 저장소의 **새 브랜치**로 진행 후 PR 병합 | `356a460` 이 조상으로 자동 포함되어 T07-C78 충족 | 새 저장소 `git init` → 조상 커밋이 없어 T07-C78 탈락 |
| 2026-09-01 | 관찰 지표를 **하루 시간 오차율(%)** 하나로 고정 | 과제 6이 이미 예상 분·실제 분을 분 단위로 보관 → 새 자료 구조 없이 5일 비교 가능 | 완료율 → 계획 규칙(시간 상한) 변경 효과가 드러나지 않음 |
| 2026-09-01 | **과제 6 DB 를 그대로 공유**한다 (사용자 결정) | 나중에 PR 로 `main` 에 합칠 때 자료가 갈라져 있으면 안 됨 | 새 DB + 내보내기 가져오기 → 병합 시 두 벌의 자료를 합쳐야 함 |
| 2026-09-01 | 소유자 컬럼에 **임시 `DEFAULT`** 를 걸고 4단계로 마이그레이션 | 같은 DB 를 쓰는 과제 6 배포가 `user_id` 없이 INSERT 해도 깨지지 않게. PR 병합 후 `DROP DEFAULT` | 바로 `NOT NULL` → 과제 6 앱의 등록 기능이 즉시 깨짐 |
| 2026-09-01 | **소셜 로그인 추가**(구글·카카오), 단 **비밀번호 로그인은 유지** (사용자 요청) | 카드 2(T07-C101~C107)가 저장된 비밀번호의 모습과 두 계정 해시 상이를 증거로 요구 → 소셜만 쓰면 증명 불가 | 소셜 전용 → 카드 2 전체를 증명할 수 없음 |
| 2026-09-01 | 소셜은 **OIDC 로 통일**, `openid-client` v6 사용 | 구글·카카오 모두 OIDC 지원. PKCE·state·id_token 서명 검증이 라이브러리에 들어 있음 | `arctic` → 2026-07 지원 중단. raw fetch 직접 구현 → 보안 경로에 자체 코드 증가 |
| 2026-09-01 | `app_user.email` 과 `login_id` 를 **NULL 허용**으로 | 카카오 이메일은 비즈 앱 전환 + 심사(영업일 3~5일)가 있어야 받을 수 있다. 소셜 전용 계정은 아이디·이메일 없이 만들어질 수 있어야 한다 | 둘 다 NOT NULL → 카카오 로그인이 심사 전에는 아예 동작하지 않음 |
| 2026-09-01 | 소셜 계정 연결은 **`email_verified` 가 참일 때만** | 미인증 이메일로 자동 연결하면 남의 계정을 가로챌 수 있다 | 이메일만 같으면 무조건 연결 → 계정 탈취 경로 |
| 2026-09-01 | **아이디와 이메일을 분리**하고 **닉네임**을 따로 저장 (사용자 결정) | 로그인은 아이디로, 표시는 닉네임으로. 이메일은 비밀번호 찾기·소셜 연결용으로 보관 | 이메일을 아이디로 사용 → 소셜 전용 계정이 이메일 없이 생기면 로그인 수단이 소셜로만 남음 |
| 2026-09-01 | 중복확인은 **입력 중 확인 + DB 유니크 인덱스** 두 겹 (사용자 결정) | 화면 확인은 편의이고 실제 보장은 DB. 확인과 제출 사이에 남이 먼저 가입하는 경우까지 막힌다 | 화면 확인만 → 동시 가입 시 중복 통과. 제출 시에만 검사 → 폼을 다 채운 뒤에야 알게 됨 |
| 2026-09-01 | 중복확인 API 는 **아이디·닉네임만** 확인해 준다 | 이메일까지 열어 주면 누가 가입했는지 캐낼 수 있다 | 이메일도 확인 → 가입자 이메일 존재 여부 유출 |
| 2026-09-01 | 비밀번호는 **10자 이상 + 대문자·소문자·숫자·특수문자 각 1자 이상** (사용자 요청) | 무차별 대입 방어가 없는 상태에서 추측 난이도를 올리는 유일한 수단 | 길이만 검사 → 흔한 단어 비밀번호가 그대로 통과 |
| 2026-09-01 | **MVC 계층 구조 + DTO 를 도입한다** — Controller(`route.ts`) / Service(`lib/service/`) / Repository(`lib/repository/`) / Domain(`lib/domain/`) / DTO(`lib/dto/`) (사용자 요청, 포트폴리오 관점) | 인증·인가는 흩어지면 한 군데만 빠뜨려도 뚫린다. **Repository 의 모든 함수가 `user_id` 를 인자로 받게** 하면 소유자 조건을 빠뜨릴 자리 자체가 없어지고, "남의 자료가 어디서 막히나요"에 계층 한 줄로 답할 수 있다. DTO 화이트리스트는 `SELECT *` 가 새 컬럼을 응답에 실어 나르는 사고를 막는다 (T07-C105 / C106) | DTO 한 파일만 두기 → 차단 지점이 여전히 라우트마다 흩어져 설명이 어렵다. 계층 없이 유지 → 파일은 적지만 새 라우트를 추가할 때 소유자 조건을 빠뜨릴 수 있다. **비용은 파일 수 증가이며, 설명 가치가 그보다 크다고 판단** |

## 4. 작업 로그

| 날짜 | 작업자 | 내용 |
|---|---|---|
| 2026-09-01 | Claude Code | `law.md` → `_ai/claude/INSTRUCTIONS.md` → `_shared/SECURITY.md` 확인, 과제 6 소스·스키마·git 이력 분석 |
| 2026-09-01 | Claude Code | 과제 6 저장소를 클론(이력 보존) 후 `plan-do-see-diary_to7` 브랜치 생성. `356a460` 이 조상임을 `git merge-base --is-ancestor` 로 확인 |
| 2026-09-01 | Claude Code | 복사된 과제 6 문서·증거·보고서 전량 제거(사용자 승인). 소스 코드는 보존 |
| 2026-09-01 | Claude Code | 과제 7 표준 문서 세트 구축 — 기획, 요구사항 매핑(68개), 인증 구현 설명서 골격, 5일 사용 기록 템플릿, 배포·검증·트러블슈팅·AI 3줄·포트폴리오, AI 진입 지침 3종 |
| 2026-09-01 | Claude Code | 사용자 결정 반영: 과제 6 DB 공유, 소셜 로그인(구글·카카오) 추가, 비밀번호 복잡도 규칙, 아이디·닉네임 분리와 중복확인, 출력 DTO 도입. 카카오 이메일 정책과 OAuth 라이브러리 현황을 조사해 설계에 반영 |

## 5. 남은 문제 / 확인 필요

- **카카오 이메일**: `account_email` 동의항목은 비즈 앱 전환 + 비즈니스 정보 심사(영업일 3~5일)를 거쳐야 한다. 심사 전에는 카카오가 이메일을 주지 않으므로 계정 연결이 안 되고 새 계정이 만들어진다.
- **카카오 OIDC 활성화**: 카카오 디벨로퍼스에서 OpenID Connect 를 켜야 `id_token` 이 나온다.
- **Redirect URI**: 로컬(`http://localhost:3000/...`)과 배포 주소 두 벌을 구글·카카오 양쪽에 모두 등록해야 한다.
- `bcryptjs` 와 `openid-client` 설치 버전을 확정해 `docs/인증_구현_설명서.md` 1항의 `(설치 버전 기입)` 을 채워야 한다 (T07-C92).
- **PR 병합 전까지** 과제 7 자료가 과제 6 주소에서 로그인 없이 보인다. A7 직후 A8(병합)을 미루지 않는다.
- `scripts/` 에 남아 있는 과제 6 보고서 생성용 PowerShell 스크립트는 A10 때 재사용하거나 정리한다.
- 5일 기록(A9)은 실제 날짜 5일이 필요하므로 A7 배포 완료 시점부터 최소 5일이 걸린다.

## 6. 다음 작업자에게

**Next Agent: Codex — A1~A5 구현**

사용자는 그 사이 구글 Cloud Console 과 카카오 디벨로퍼스에서 앱을 만들고 Redirect URI 를 등록한다.

### 다음 작업자용 실행 지시 프롬프트

```
작업 폴더: C:\Users\stuls\Desktop\Agent\plan-do-see-diary_to7
브랜치: plan-do-see-diary_to7 (main 에 직접 커밋하지 않는다. 나중에 PR 로 합친다)
DB: 과제 6 데이터베이스를 그대로 공유한다 (.env.local 의 DATABASE_URL)

먼저 읽을 것 (이 순서대로, 폴더 전체를 훑지 말 것):
  1) 작업내역_체크리스트.md        (이 파일 - 현재 상태와 결정)
  2) docs/01_기획.md               (설계 기준. 여기서 벗어나지 말 것)
  3) docs/00_과제_요구사항_매핑.md (통과 기준 68개)

할 일: A1~A5 구현

A1  schema_auth.sql 작성 (기획 4-1, 4-3)
    - app_user(id, login_id, nickname, email, password_hash, created_at, updated_at)
      login_id / email / password_hash 는 NULL 허용, nickname 은 NOT NULL
      lower() 기준 유니크 인덱스 3개 (login_id, nickname, email)
    - user_identity(provider, provider_user_id, user_id, email_at_link, created_at)
    - user_session(token_hash, user_id, expires_at, created_at)
    - plan/task/review 소유자 컬럼은 반드시 4단계 순서로:
      (1) NULL 허용 추가 -> (2) 기존 행을 소유자 계정으로 UPDATE -> (3) 임시 DEFAULT -> (4) NOT NULL
      3단계 DEFAULT 에는 "PR 병합 후 DROP DEFAULT" 주석을 반드시 남긴다.

A2  npm i bcryptjs @types/bcryptjs
    lib/service/auth.ts     - checkLoginIdRule / checkNicknameRule / checkPasswordRule,
                      bcrypt(cost 12) 해시·검증, 정규화, 공통 실패 문구 상수
                      비밀번호: 10자 이상 + 대문자·소문자·숫자·특수문자 각 1자 이상 (서버에서 반드시 재검사)
    lib/session.ts  - randomBytes(32) 토큰 발급, DB 에는 sha256 만 저장, 만료 7일,
                      requireUser / destroySession / destroyAllSessions
    middleware.ts   - 쿠키 없으면 /login (보안 판단은 여기서 하지 않음)
    app/(auth)/signup - 아이디·닉네임·이메일·비밀번호 4칸, 아이디·닉네임 입력 중 중복확인
    app/(auth)/login  - 아이디·비밀번호
    app/api/auth/check   - 아이디·닉네임 사용 가능 여부만 응답 (이메일은 확인해 주지 않는다)
    app/api/auth/signup|login|logout

A3  npm i openid-client
    lib/service/oauth.ts    - 구글(accounts.google.com), 카카오(kauth.kakao.com) discovery,
                      인가 URL 생성(state + PKCE), 콜백에서 id_token 검증
    lib/repository/identity.ts - 계정 연결 규칙(기획 4-2). email_verified 가 참일 때만 기존 계정에 연결.
                      소셜 닉네임이 겹치면 뒤에 숫자를 붙여 비어 있는 것을 찾는다.
    app/api/auth/oauth/[provider]/route.ts 와 .../callback/route.ts
    환경변수: GOOGLE_CLIENT_ID/SECRET, KAKAO_CLIENT_ID/SECRET (NEXT_PUBLIC_ 절대 금지)
    제공자의 액세스 토큰·id_token 은 저장하지 않는다.

A4  계층 구조로 정리하면서 소유권을 강제한다 (기획 6-A).
    lib/domain/   - planner.ts, query.ts 를 여기로 이동(내용 변경 없음),
                    rules.ts(아이디·닉네임·비밀번호 규칙), metric.ts(오차율·결측·이상치·반올림).
                    DB 와 HTTP 를 import 하지 않는 순수 함수만 둔다.
    lib/repository/ - user, session, identity, plan, task, run, review.
                    SQL 은 전부 여기에만 있다.
                    조회·수정·삭제 함수는 예외 없이 user_id 를 인자로 받는다.
                    한 건 조회·수정·삭제는 WHERE id=$1 AND user_id=$2, 0행이면 서비스가 404 로 만든다.
    lib/service/  - auth, oauth, plan, task, review, account. 트랜잭션은 여기서만 연다.
    lib/dto/      - publicUser / me / publicPlan / publicTask / publicRun / publicReview.
                    응답에 나갈 필드를 화이트리스트로 명시한다.
    app/api/**/route.ts 는 요청 해석 -> requireUser() -> 서비스 호출 -> DTO 응답만 한다. SQL 금지.
    app/**/page.tsx 는 첫 줄에 requireUser(), 서비스 결과를 그리기만 한다.
    buildTaskWhere() 첫 조건에 t.user_id = $1 을 무조건 넣는다.
    사용자 ID 를 URL·헤더·본문에서 읽는 코드는 만들지 말 것.
    /api/export 의 SELECT * 도 Repository + DTO 를 거치게 바꾼다.

A5  app/account 화면 - 닉네임 변경(중복 검사), 비밀번호 변경(전 세션 폐기),
    계정 삭제(트랜잭션, 화면에 삭제 안내), /api/export 소유 제한

지켜야 할 것:
  - 로그인 실패 문구는 "아이디 없음"과 "비밀번호 틀림"이 완전히 같아야 한다
    ("아이디 또는 비밀번호가 올바르지 않습니다." + 401). 가입 단계의 중복 문구는 구체적으로 알려 줘도 된다.
  - 비밀번호와 클라이언트 시크릿을 로그·응답·화면 어디에도 남기지 않는다. 요청 본문 통째 로깅 금지.
  - 중복확인을 화면에서만 하지 말고 DB 유니크 인덱스로 반드시 막는다.
  - 계층 규칙을 지킨다: Controller 에 SQL 금지, Repository 에 쿠키·요청·응답 금지,
    트랜잭션은 Service 에서만, 응답은 반드시 DTO 경유, Domain 은 DB·HTTP 를 import 하지 않는다.

검증: npm test / npm run typecheck / npm run build / npm audit 전부 통과시킬 것.
      test/ 에 추가할 것: 아이디·닉네임 형식 검사, 비밀번호 복잡도(조건별로 하나씩 빠진 값이 모두 거절되고
      다 갖춘 값은 통과), 같은 비밀번호 두 계정의 해시가 다름, buildTaskWhere 가 user_id 조건을 반드시 포함,
      세션 만료 판정, 계정 연결 규칙, DTO 가 password_hash 를 절대 내보내지 않음, 오차율 반올림·결측 규칙.
      계층 규칙 검사도 넣는다: app/api/** 에 SQL 문자열이 없고, lib/domain/** 이 db 를 import 하지 않는다.

끝나면: 작업내역_체크리스트.md 의 진행 체크리스트·작업 로그를 갱신하고,
        docs/인증_구현_설명서.md 1항과 3항을 실제 구현·설치 버전과 일치시킨 뒤 보고한다.
```
