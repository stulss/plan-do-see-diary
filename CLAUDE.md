# CLAUDE.md — 플랜두씨 다이어리

## 읽는 순서

1. 워크스페이스 헌법 `../law.md`
2. 이 파일
3. `작업내역_체크리스트.md` (진행 상황·결정·다음 할 일의 단일 진실 공급원)
4. 지금 하는 작업과 직접 관련된 문서 1~2개만 (`docs/01_기획.md` 또는 `docs/00_과제_요구사항_매핑.md`)

## 토큰 절약

- 폴더 전체 훑기 금지. 진행 상황은 `작업내역_체크리스트.md` 한 파일로 확인한다.
- `node_modules/`, `.next/`, `.git/`은 읽지 않는다.
- `docs/01_기획.md`는 목차에서 필요한 장만 잘라 읽는다.

## 이 프로젝트의 불변 규칙

1. 시간 단위는 전 구간 '분'. 저장·계산·표시 모두 동일.
2. 마감일·계획 기간은 `date`(서울 날짜), 시각은 `timestamptz`(UTC 저장 → 서울 표시).
   "오늘"은 `(now() AT TIME ZONE 'Asia/Seoul')::date`.
3. 할 일 삭제는 `deleted_at` 소프트 삭제. 집계는 항상 `deleted_at IS NULL`.
4. 완료 중복 방지는 `task_completion.task_id` 기본키 + `ON CONFLICT DO NOTHING`.
   버튼 비활성화로 막지 않는다.
5. 계획 수정 이력은 DB 트리거가 `plan_revision`에 적재한다. 애플리케이션 코드에서 처리하지 않는다.
6. 실행 기록 저장은 `plan`/`task`를 절대 수정하지 않는다.
7. 집계 숫자와 되짚기 목록은 같은 조건 생성 함수를 공유한다.
8. DB 접속 문자열은 서버 전용. `NEXT_PUBLIC_*` 금지.
9. 실제 자료(계획·할 일·실행 기록)를 AI가 지어내지 않는다. 사용자가 직접 입력한다.

## 작업 종료 조건

코드 변경 후 `작업내역_체크리스트.md`의 작업 로그와 진행 체크리스트를 갱신해야 그 작업이 끝난 것이다.

---

## 과제 7 (인증) 작업 시

이 저장소는 과제 6과 과제 7을 함께 담는다. **과제 6 문서·보고서·증거는 지우지 않는다.**

| 종류 | 과제 6 | 과제 7 |
|---|---|---|
| 진행 기록 | `작업내역_체크리스트.md` | **`과제7_작업내역_체크리스트.md`** |
| 문서 | `docs/*.md` | **`docs/과제7/*.md`** |
| 증거 | `docs/evidence/` | **`docs/과제7/evidence/`** |

과제 7 작업은 `과제7_작업내역_체크리스트.md` 를 먼저 읽고, 설계는 `docs/과제7/01_기획.md` 를 따른다.

### 과제 7 불변 규칙

1. 로그인 방법은 **아이디 + 비밀번호 하나**다.
2. 사용자 ID 는 **세션에서만** 얻는다. URL·헤더·요청 본문에서 읽지 않는다.
3. 목록·검색·거르기·집계는 모두 `lib/domain/query.ts` 의 `buildTaskWhere` 를 지나며, 첫 조건은 `user_id` 다.
4. 한 건 조회·수정·삭제는 `WHERE id AND user_id` 로 하고 0행이면 404 다. 소유자 조건을 UPDATE/DELETE 의 WHERE 에 함께 두어 거절이 자료를 바꾸지 못하게 한다.
5. 비밀번호는 `bcryptjs`(작업 계수 12)에 맡긴다. 규칙 검사는 `lib/domain/rules.ts` 한 곳에서만 하고 서버에서 반드시 재검사한다.
6. 세션은 불투명 난수이고 DB 에는 `sha256` 만 저장한다. 로그아웃은 DB 행 삭제다.
7. 응답은 `lib/dto/` 를 거친다. DB 행을 그대로 반환하지 않는다.
8. 소유자 컬럼 마이그레이션은 순서를 지킨다: NULL 허용 → 백필 → 임시 DEFAULT → NOT NULL. 배포 후 DROP DEFAULT.
